#need to import all other views in this folder

#def browser_history_user(user):
#    bashh_istory = Logs.Object.All
#   fields = ['time','command']


   # line_chart_maker(queryset=queryset, fields=fields)


